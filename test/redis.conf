# Don't daemonize, since we want the logs to go to stdout
daemonize no

# Only listen on localhost. We'll expose Redis over SSL via stunnel for other
# traffic.
bind 127.0.0.1

# JC writes to db 1 of the GJ Redis.
databases 2
